//load the server status file, and save it time to time
import { statusMapping } from "./initialize";
import fs from "node:fs";

async function saveStatusMappingOnInterval(){
    
}

export default {saveStatusMappingOnInterval}